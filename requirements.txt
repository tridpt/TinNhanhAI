Flask==3.1.3
requests==2.34.2
feedparser==6.0.14
beautifulsoup4==4.13.3
ddgs==9.14.2
# diskcache 5.6.3 is the latest release. CVE-2025-69872 (pickle RCE) has no
# fixed version yet; it only applies when an attacker already has write access
# to the local .cache/ dir, which is not exposed here. Bump when a fix ships.
diskcache==5.6.3
waitress==3.0.2
